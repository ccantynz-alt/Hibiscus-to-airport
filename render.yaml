services:
  - type: web
    name: hibiscustoairport-backend
    runtime: docker
    plan: starter
    # Render will detect Dockerfile at repo root by default
    # dockerfilePath: ./Dockerfile
    # dockerContext: .
    envVars:
      - key: PORT
        value: "10000"
    healthCheckPath: /debug/stamp
