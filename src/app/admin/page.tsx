'use client';

export default function AdminPage() {
  return (
    <div style={{ padding: 32, fontFamily: 'system-ui, Segoe UI, Arial' }}>
      <h1>ADMIN PANEL PROOF</h1>
      <p><b>STAMP:</b> ADMIN_FORCE_RENDER_20260208</p>
      <p>If you can see this, /admin is rendering and JS executed.</p>
      <div style={{ marginTop: 16, padding: 16, border: '1px solid #ccc' }}>
        <div>Next step: wire cockpit UI + backend status calls.</div>
      </div>
    </div>
  );
}