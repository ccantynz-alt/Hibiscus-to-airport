import React from "react";

export default function SafeBookings({ mounted }) {
  return (
    <div style={{ padding: 28 }}>
      <h1>Bookings Admin (Fallback)</h1>
      <p><b>STAMP:</b> HIBI_MAJOR_005_RESTORE_BOOKINGS_AND_RESET_20260209</p>
      <p><b>Mounted target:</b> {mounted || "(none)"} </p>
      <p>Your real bookings dashboard component could not be detected or failed to load.</p>
      <p>Paste the TOP BOOKINGS CANDIDATES table output and we will pin the exact file next.</p>
    </div>
  );
}