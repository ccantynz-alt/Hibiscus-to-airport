import React from "react";

export default function SafeBookings() {
  return (
    <div style={{ padding: 28 }}>
      <h1>Bookings Admin</h1>
      <p><b>STAMP:</b> HIBI_FINAL_AUTH_REAL_BOOKINGS_COCKPIT_004_20260209</p>
      <p>This is a safe fallback bookings panel while we wire the real one.</p>
      <p>If your real bookings component crashes, you will see the error instead of a white screen.</p>
    </div>
  );
}